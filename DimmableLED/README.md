Used a potentiometer to dim an LED in real time. Learned how a potentiometer works internally, it's basically a voltage divider where turning the knob moves a wiper across a resistive strip, giving you a smoothly varying voltage output. The Arduino reads that with analogRead() (0–1023), and I mapped it to the PWM range (0–255) using a linear equation to drive the LED brightness with analogWrite().
